# 官网本地化规则

官网仍由 Cloudflare Pages 从仓库根目录直接部署，不在 Pages 增加构建服务。发布前在本地运行确定性生成器，把四语页面作为静态文件提交：浏览器、搜索引擎和分享抓取器读到的首个 HTML 就是对应语言，不依赖客户端翻译。

## 内容与生成物

- 根目录 `index.html`、`welcome.html`、`updates.html`：页面结构的唯一编辑入口，同时也是无 JavaScript 时可阅读的英文中立入口。新增用户可见文案应加 `data-key`，替代文本用 `data-alt-key`，读屏名称用 `data-aria-label-key`；不要单独编辑生成页中的译文。
- `components/navbar.html`、`components/footer.html`：共享结构。品牌、平台名、用户数据字段和文件格式保持原名。
- `locales/{en,zh-CN,ja,es}.json`：共享与各页词条，以及每页标题、描述。词条是受信任的仓库内容；含 HTML 的词条须人工审阅标记，不接受用户输入。
- `updates-data.json`：全部历史版本记录的唯一数据文件。新记录的日期、摘要和功能标题/说明须有四语；旧记录只翻译当时的事实。
- `scripts/build-localized-pages.mjs`：生成根目录中立英文入口、`en/`、`zh-CN/`、`ja/`、`es/` 各三页和 `sitemap.xml`。这些 HTML 与 sitemap 是派生部署资产，必须与源文件一同提交。`updates-renderer.js` 是更新记录的共同渲染器，生成器与浏览器的动态刷新共用它。

四种语言各有稳定地址：`/{locale}/`、`/{locale}/welcome`、`/{locale}/updates`。根目录 `/`、`/welcome`、`/updates` 是兼容和自动语言选择入口，不是任何语言的规范地址。实际仓库文件带 `.html`，Cloudflare Pages 提供无扩展名访问。每个语言页面有自指 canonical、四语互相指向的 `hreflang`，`x-default` 指向对应中立入口；sitemap 只列 12 个规范地址。每种语言的标题、描述、Open Graph、正文及首页结构化 FAQ 都写在该地址的 HTML 中。

## 语言选择与回退

明确的语言路径优先；旧 `?lang=` 链接其次；随后是用户手动保存的官网偏好、浏览器语言；不支持的语言最终用英文。`ja-JP`、`es-MX` 等地区变体归入基础语言。客户端把旧入口或自动检测结果导航到规范语言路径；只有用户在菜单中切换语言才保存偏好。内部链接始终指向当前语言的静态页面。

生成时缺少词条会失败，不能发布半译页面。运行时英文词包缺失时保留已经生成的静态页面；某个非英语词包加载失败时也保留该语言页面与导航，不错误覆盖为英文。浏览器脚本不是首屏译文的前提。新增语言应补齐一个词包，并更新支持列表、生成器、测试与 sitemap；不要复制页面脚本。

业务逻辑和埋点不依赖翻译后的按钮文字；链接目标等稳定语义决定事件，`ui_language` 只是分析维度。只有规范语言页产生 GA 的 `page_view`，旧入口跳转不计入一次额外浏览。

## 发版与验收

浏览器插件的 `doc/release-entry-{version}.json` 是新版本四语记录与中英商店说明的共同输入；插件仓 `scripts/prepare_release.py --write` 只更新官网 `updates-data.json`，不会擅自改官网 HTML。之后在官网仓执行：

```sh
npm ci
npm run build:i18n
npm test
npm run check:i18n
```

核对 `updates-data.json` 与派生的四语 `updates.html` 一起进入官网提交，再推送官网 `main`；生产部署后逐语言检查首响应、版本条目、语言切换和错误路径的 404。Cloudflare Pages 本身仍无构建命令。当前 v1.5.0 及更早历史条目已补齐四语，不重打旧商店包或改写旧 Tag。

每次改译文，让未参与翻译的人以当地读者视角检查事实、语气、术语和窄屏排版；键位齐全不代表译文自然。本地可用 `npx wrangler pages dev . --port 43135 --compatibility-date=2026-04-12` 或其他模拟静态路径的服务器验收；普通 `python3 -m http.server` 不提供 Pages 的无扩展名路由。浏览器检查首页、欢迎页、更新记录、语言切换、导航及窄屏。

本契约只覆盖官网四语页面及其搜索/分享首响应，不等于建立了 Markdown 版网页、`llms.txt` 或 Agent 内容协商能力。
